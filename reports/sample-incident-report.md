# Incident Report — Lab observation, 2026-04-29

**Run**: phantom-secops blue-side pipeline, parallel to a red-team kill-chain demo.
**Window**: 14:32:00 — 14:33:02 UTC (62 seconds).
**Affected lab assets**: `juice-shop`.
**Author**: blue-incident-report agent.

---

## TL;DR

Reconnaissance and vulnerability scanning were observed against `juice-shop`
within the lab network. Activity was attributable to a single actor (lab IP
`172.20.0.x`, the attacker container by design). The blue-team pipeline
detected scanner activity within 12 seconds of the first probe and correlated
all subsequent activity to the same actor. No production assets were
involved.

---

## Timeline

| Time (UTC)    | Event                                                              |
|---------------|--------------------------------------------------------------------|
| 14:32:04      | First TCP SYN burst observed against juice-shop:3000               |
| 14:32:12      | log-anomaly agent flags scanner UA pattern (P3 severity hint)      |
| 14:32:15      | alert-triage promotes to P2 after 5+ probes in 3s                  |
| 14:32:19      | First Nuclei template match recorded (admin-panel exposure)        |
| 14:32:30      | threat-correlate agent links all 24 alerts to single source IP     |
| 14:32:48      | Path-traversal probe pattern detected (no successful traversal)    |
| 14:33:02      | Activity ends; incident report finalized                           |

---

## Affected Assets

| Asset | Criticality | Status |
|---|---|---|
| `juice-shop` (lab container) | low (lab) | observed; no exploitation |

No other assets were touched.

---

## Actor Summary

**Actor A** — lab address `172.20.0.x` (the secops-attacker container, by design).

Behavior consistent with phases:

- **TA0043 Reconnaissance** — port scan of juice-shop:3000.
- **TA0007 Discovery** — vulnerability scanning via nuclei + nikto templates.
- **TA0001 Initial Access (attempted, no success)** — XSS- and SQLi-shaped
  request strings observed against `/rest/products/...`. None resulted in 5xx
  or unusual response patterns suggesting successful injection.

**Confidence**: high (single source, lab environment, full session captured).

---

## Indicators of Compromise

| Type | Value | Notes |
|---|---|---|
| IP   | 172.20.0.x | attacker container, single source |
| User-Agent | `Nuclei -`, `Nikto/2.5`  | scanner identification, not spoofed |
| URI patterns | `../`, `union+select`, `<script>` | classifier hits |

In a real engagement, the IP would be a real source; in this lab, the source
is the attacker container, which is part of the test design.

---

## Recommended Follow-up

For an application like Juice Shop running in production (it should not — it's
intentionally vulnerable), the follow-up would be:

1. **Block the source IP at edge.** Standard SOC playbook.
2. **Apply the patches identified by the red side**: jQuery upgrade,
   admin-panel access control. See the sibling pentest report.
3. **Validate WAF coverage** — the path-traversal patterns reached application
   logs, which suggests no WAF (or a misconfigured one).

For the lab itself, the takeaway is:

- **Detection latency was 12s** from first probe to first triaged alert.
- **Correlation latency was 30s** from first probe to single-actor attribution.
- **Both are bounded by the agent polling cadence** (set to 5s in this demo).
  Tighter cadence would reduce both.

---

## Comparison to red-side report

A side-by-side reading is recommended. Red side's "Methodology → vuln-scan
phase begins at t+10s" aligns with blue side's "first scanner UA detection at
t+12s" — a 2-second detection gap. The full pentest report finalized at
t+62s; the incident report finalized at t+62s. **The defender keeps pace with
the attacker when the analysis pipeline runs concurrently.**

---

*Report generated by phantom-secops blue-incident-report agent.*
